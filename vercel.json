{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci --prefix game-client",
  "buildCommand": "npm run build --prefix game-client",
  "outputDirectory": "game-client/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
